def count_in_list(lst, item):
    """
    Count occurrences of an item in a list.

    Parameters:
    lst (list): List of elements.
    item (any): Item to count in the list.

    Returns:
    int: Number of occurrences of the item in the list.
    """
    return lst.count(item)
